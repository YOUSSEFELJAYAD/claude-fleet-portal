interface:
  display_name: "Claude Fleet Portal"
  short_description: "Repo-specific patterns and workflows for claude-fleet-portal"
  default_prompt: "Use the claude-fleet-portal repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true