// PLACEHOLDER — replaced by Slice 06 (manager-loop)
// Exports the minimal signature that loops.ts calls via dynamic import.

export async function runManagerLoop(loop: any, project: any, cp: any): Promise<any[]> {
  return [];
}
